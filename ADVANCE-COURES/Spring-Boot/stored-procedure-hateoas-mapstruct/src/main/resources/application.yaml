server:
  port: 9080

spring :
  datasource:
    url: jdbc:postgresql://localhost:5432/testSpringHateoas
    username: postgres
    password: Hai172212
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
#    databasePlatform:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
